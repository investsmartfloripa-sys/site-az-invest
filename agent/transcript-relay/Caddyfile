# HTTPS automático (Let's Encrypt) na frente do relay de transcrição.
# DOMAIN vem do .env — com sslip.io não precisa configurar DNS nenhum:
# um VPS de IP 203.0.113.10 vira o domínio 203-0-113-10.sslip.io.
{$DOMAIN} {
	reverse_proxy transcript-relay:8000
}
