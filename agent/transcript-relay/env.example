# Copie para .env e preencha (standalone; na opção B estes vão no .env da Evolution).

# Chave de acesso do relay — gere com: openssl rand -hex 24
RELAY_TOKEN=

# Domínio HTTPS. Sem DNS próprio, use sslip.io: IP com traços + .sslip.io
# (ex.: VPS 203.0.113.10 → 203-0-113-10.sslip.io)
DOMAIN=
